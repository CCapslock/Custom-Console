using UnityEngine;

[CreateAssetMenu(fileName = nameof(Grafity), menuName = "DataStore/" + nameof(Grafity), order = 0)]
public sealed class Grafity : ScriptableObject
{
    //написать при скрешивании веток?? 
}